[project]
name = "pdf-to-text"
version = "0.1.0"
description = "A quick tool to convert PDF files to text"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pdf2image",
    "Pillow",
    "pytesseract"
]
